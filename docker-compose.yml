# docker-compose.yml
version: "3.8"
services:
  flutter-web:
    image: nginx:alpine
    volumes:
      - ./build/web:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "5000:80"
    restart: unless-stopped
