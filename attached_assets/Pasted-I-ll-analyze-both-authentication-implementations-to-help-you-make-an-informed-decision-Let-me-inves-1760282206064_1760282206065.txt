I'll analyze both authentication implementations to help you make an informed decision. Let me investigate their structure, purpose, and usage patterns.


Planned authentication removal analysis
Perfect! I've completed my investigation. Here's a comprehensive analysis of the three authentication implementations in your codebase:

📊 Authentication Implementation Analysis
Implementation #1: lib/core/app_logic/ ✅ CURRENTLY ACTIVE
Architecture: Clean Architecture with Riverpod
Status: ✅ ACTIVELY USED

Structure:

lib/core/app_logic/
├── domain/
│   ├── entities/ (User, AuthState, AuthResult - Freezed)
│   ├── repositories/ (AuthRepository interface)
│   └── usecases/ (LoginUseCase, LogoutUseCase, RegisterUseCase, etc.)
├── data/
│   ├── dtos/ (AuthDtos, UserDtos - Freezed)
│   ├── mappers/ (AuthMapper, UserMapper)
│   ├── datasources/ (AuthRemoteDataSource, AuthLocalDataSource)
│   └── repositories/ (AuthRepositoryImpl)
└── services/
    ├── auth_service.dart
    └── google_signin_service.dart
Usage:

✅ Used by AuthWrapper (main entry point)
✅ Used by LoginScreen (actual login UI)
✅ Integrated with Riverpod providers in lib/di/login_providers.dart
✅ Handles session persistence with SharedPreferences
✅ Supports: Email/password, Demo login, Google OAuth
✅ This is your PRODUCTION authentication system
Implementation #2: lib/features/authentication/ ❌ UNUSED
Architecture: Clean Architecture with BLoC/Cubit
Status: ❌ NOT ACTIVELY USED

Structure:

lib/features/authentication/
├── domain/
│   ├── entities/ (UserEntity, AuthTokensEntity, AuthResultEntity)
│   ├── repositories/ (AuthRepository interface)
│   └── usecases/ (EmailLoginUseCase, GoogleLoginUseCase, etc.)
├── data/
│   ├── models/ (UserModel, AuthTokensModel - JSON serializable)
│   ├── datasources/ (AuthRemoteDataSource, MockAuthDataSource)
│   ├── repositories/ (AuthRepositoryImpl)
│   └── services/ (GoogleSignInService, MockDataService)
└── presentation/
    ├── cubit/ (AuthCubit, FeatureFlagCubit)
    ├── pages/ (LoginPage, RegisterPage, ForgotPasswordPage, etc.)
    └── widgets/ (EmailLoginForm, GoogleLoginButton, etc.)
Usage:

⚠️ Only routes in app.dart reference this: /register, /forgot-password, /reset-password
⚠️ Has full UI pages but AuthCubit is never instantiated in the app
⚠️ Has duplicate AuthService and GoogleSignInService
⚠️ Uses BLoC pattern (while rest of app uses Riverpod)
Implementation #3: lib/features/login/ ✅ ACTIVELY USED
Architecture: Presentation layer only (uses #1's services)
Status: ✅ ACTIVELY USED

Structure:

lib/features/login/presentation/
├── pages/
│   ├── auth_wrapper.dart  ✅ Main app entry
│   └── login_screen.dart  ✅ Login UI
└── widgets/
    ├── modern_login_form.dart
    ├── google_signin_button.dart
    ├── developer_controls_panel.dart
    └── animated_login_elements.dart
Usage:

✅ AuthWrapper is the app's home widget (MaterialApp.home)
✅ LoginScreen is the actual login interface users see
✅ Consumes services from Implementation #1 (lib/core/app_logic/)
✅ This is your PRODUCTION UI layer
🎯 Recommendation: Remove Implementation #2
Why Remove lib/features/authentication/?
❌ Problems:

Completely unused - AuthCubit is never instantiated
Duplicate code - Reimplements everything in lib/core/app_logic/
Wrong pattern - Uses BLoC while app uses Riverpod
Orphaned routes - Register/ForgotPassword pages reference it but show "not implemented"
Technical debt - Adds ~50+ files with zero active usage
✅ What to Keep:

Keep lib/core/app_logic/ - Your active Clean Architecture auth backend
Keep lib/features/login/ - Your active presentation layer (UI)
Move useful UI from /authentication/ to /login/ if needed
✅ What Happens to Routes:
The /register, /forgot-password, /reset-password routes currently point to unused pages. You can either:

Delete them (they're not implemented anyway)
Recreate them in /features/login/ using the active auth services
📝 Decision Time
Would you like me to:

✅ Remove lib/features/authentication/ entirely (recommended)
🔄 Salvage any UI components from /authentication/ before removing
🧹 Clean up orphaned routes in app.dart
Let me know your preference and I'll execute the cleanup! 🚀